spring.application.name=coduel-app
server.port=8080
server.servlet.context-path=/coduel
logging.config=classpath:log4j2-spring.xml
logging.level.root=INFO
logging.level.com.coduel=DEBUG

# ---- Code execution ----
# pool-size: 0 = auto (number of available CPUs)
execution.pool-size=0
execution.queue-capacity=50
execution.default-timeout-ms=5000
execution.max-timeout-ms=10000

# ---- Actuator ----
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# ---- Persistence (NOT ACTIVE YET) ----
# No JPA on the classpath yet. Uncomment + add spring-boot-starter-data-jpa to
# coduel-common when the Problem/Submission slice lands, and set the password via
# the CODUEL_DB_PASSWORD env var (never hardcode it here).
#spring.datasource.url=jdbc:mysql://localhost:3306/coduel
#spring.datasource.username=coduel
#spring.datasource.password=${CODUEL_DB_PASSWORD}
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
