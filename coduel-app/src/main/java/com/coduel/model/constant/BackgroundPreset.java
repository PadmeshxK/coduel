package com.coduel.model.constant;

/** Chat background. PARCHMENT is the site default; IMAGE uses the stored backgroundImageUrl. */
public enum BackgroundPreset {
    PARCHMENT,
    SUNSET,
    BLUEPRINT,
    FOREST,
    ESPRESSO,
    AURORA,
    OCEAN,
    ROSE,
    MIDNIGHT,
    IMAGE
}
