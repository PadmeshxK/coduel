package com.coduel.model.constant;

/**
 * Message-bubble shape. All variants stay rounded — the site has no sharp edges by design — they
 * differ in corner radius / fill: ROUNDED (default), PILL (extra-round), MINIMAL (flatter fill).
 */
public enum BubbleStyle {
    ROUNDED,
    PILL,
    MINIMAL
}
